document.addEventListener('DOMContentLoaded',()=>{ const form=document.getElementById('perfilForm'); const dados=JSON.parse(localStorage.getItem('perfil')||'{}'); ['nome','email','telefone','endereco'].forEach(k=>{ const el=document.getElementById('perfil_'+k); if(el) el.value=dados[k]|| (k==='email'?localStorage.getItem('usuario_email')||'':''); }); form?.addEventListener('submit',e=>{ e.preventDefault(); const novo={nome:perfil_nome.value,email:perfil_email.value,telefone:perfil_telefone.value,endereco:perfil_endereco.value}; localStorage.setItem('perfil',JSON.stringify(novo)); alert('Perfil salvo!'); }); });
