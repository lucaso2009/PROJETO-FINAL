const API_BASE = (['8080','80','5500','5173'].includes(location.port)) ? 'http://localhost:3000' : '';
document.getElementById('adminLoginForm')?.addEventListener('submit', async e=>{ e.preventDefault(); const email=document.getElementById('adminEmail').value.trim().toLowerCase(); const senha=document.getElementById('adminSenha').value; try{ const res=await fetch(API_BASE+'/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,senha})}); const data=await res.json().catch(()=>({})); if(!res.ok){alert(data.error||'Login inválido');return} if(data.user?.tipo!=='admin' && email!=='admin@promofinder.local'){alert('Este usuário não é administrador.');return} localStorage.setItem('usuario','admin'); localStorage.setItem('usuario_email',email); location.href='ADM/dashboard.html'; }catch(err){ console.error(err); alert('Erro ao entrar no admin. Confira se o servidor está rodando.'); } });
